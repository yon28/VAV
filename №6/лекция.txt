    // => вместо {return}  

    // явное приведение (round as Disk).circle()
    // if (round is  Disk) { r =  (Disk)round;}
    // агрегация двух кругов это кольцо. Прописать площадь и т.д. (или методы наследования)
    //  : this(1)
    //  : base(radius);   - радиус родительского класса  
    // переопределение
    // отложенная загрузка

  //   public int radius {get; private set;} Если хотят изменить r, то проверить на положительность 
    //индексаторы, свойства индексаторов  
